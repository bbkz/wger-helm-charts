# external database via an existing secret; in-cluster postgres disabled
app:
  django:
    existingDatabase:
      enabled: true
      host: db.example.com
      port: 5432
      existingSecret:
        name: my-db-secret
postgres:
  enabled: false
